language: c
sudo: true
script:
  - sudo apt update
  - sudo apt-get install linux-headers-$(uname -r)
  - cd phase_2/part_1/hello_os_module/
  - sudo make
  - cat Makefile

after_success:
  - echo success